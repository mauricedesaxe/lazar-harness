[package]
name = "harness-check"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9"
regex = "1"
libc = "0.2"

[profile.release]
lto = true
strip = true
opt-level = "z"
